rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Internal team tool behind a password gate (VITE_APP_PASSWORD).
    // Firebase Auth was removed; access is gated client-side.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
